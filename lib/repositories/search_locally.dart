import 'dart:js';

import 'package:consulting_app_pailmail/providers/categories_provider.dart';
import 'package:flutter/cupertino.dart';
import 'package:provider/provider.dart';

class SearchLocally {
  searchLocally({required String searchText, required BuildContext context}) {
    // Provider.of<CategoriesProvider>(context).allCategories.data?.where((element) => false);
  }
}
