class InboxProvider {
  DateTime dateTime = DateTime.now();

  // changeDate({DateTime selectedDate}) {
  //   if (selectedDate != null && value != widget.selectedDate) {
  //     setState(() {
  //       widget.selectedDate = value;
  //     });
  //   }
  // }
}
